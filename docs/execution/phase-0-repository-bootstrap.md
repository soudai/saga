# Phase 0: Repository Bootstrap

Issue: #2

## Goal
- Go module と基本ディレクトリ構成を作り、実装を開始できる状態にする。

## Scope
- `go.mod` の初期化
- `cmd/saga`, `internal/`, `test/` の作成
- `Makefile` または `justfile` の追加
- CI ひな形の追加

## Done
- `go test ./...` が通る
- `saga version` が動作する
